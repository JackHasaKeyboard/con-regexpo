#head
  = render 'form', post: @post

#cont
  - @posts.each do |post|
    %div
      %div.head
        %h3= post.name

      %div.body
        %pre
          %code= post.expr

        %pre
          - expr = Regexp.new(post.expr)
          %code= expr.match(post.eg)
